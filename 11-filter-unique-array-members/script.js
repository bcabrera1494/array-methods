let strings = ["kopi", "joe", "kopi", "joe", "widdle", "kopi", "joe", "nub"];

function unique(arr) {
    // code to filter unique array members
    // initialize a new arary
    let uniqueStrings = new Array;
    // map through the array with a filter function
    // conditional statement: 
    // if item is not present in the new array, add item to the new array
    // if item is present in the new array, continue
    // return unique members in new array
    // return should be kopi, joe, widdle, nub
};

console.log(unique(strings));